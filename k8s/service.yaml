apiVersion: v1
kind: Service
metadata:
  name: sandata-service
  namespace: sandata
  labels:
    app: sandata
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
    name: http
  selector:
    app: sandata

